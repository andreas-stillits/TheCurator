### About hashing

- Dont include any references to time or user system in hashes. Only canonicalized contents
- Always canonicalize (sort keys, remove whitespaces, etc.)
- Always stream files in chunks - avoid loading large files into memory

